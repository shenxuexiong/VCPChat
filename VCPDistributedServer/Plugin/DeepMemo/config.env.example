# DeepMemo回忆参数相关
# VchatDataURL是Vchat的本地数据存储路径，通常不需要修改。
VchatDataURL=
# MaxMemoTokens是最终输出给模型的最大字符数，防止内容过长。
MaxMemoTokens=60000

# Rerank 重排查询相关
# 是否启用Rerank功能，填 True 或 False
RerankSearch=True
# Rerank服务的API地址，例如 http://127.0.0.1:8000/
RerankUrl=http://your-rerank-api-endpoint/
# Rerank服务的API密钥
RerankApi=sk-your-api-key-here
# 使用的Rerank模型名称
RerankModel=Qwen/Qwen3-Reranker-8B
# 发送给Rerank API的单批次最大字符数，用于防止超长请求。
RerankMaxTokensPerBatch=30000
# Rerank后返回的最相关结果数量。
RerankTopN=5

# 关键词屏蔽
# 在这里设置你希望在搜索时自动忽略的高频词或无意义词，用英文逗号隔开。
# 例如: BlockedKeywords=你好,主人,下午好

BlockedKeywords=你好,主人,下午好
