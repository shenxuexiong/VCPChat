# PowerShell Executor Plugin Configuration

# Defines the default return behavior for command outputs.
# - "delta": Returns only the output from the most recently executed command. (Default, recommended)
# - "full": Returns the entire content of the PowerShell terminal buffer after each command.
POWERSHELL_RETURN_MODE=delta

# PowerShellExecutor 插件安全配置

# --- 完全禁止的指令 ---
# 列出完全禁止的指令中的关键字。
# 注意：新版本使用智能检查机制，能够区分命令关键字和路径内容。
# 例如：路径 "H:\Down\APK\platform-tools-latest-windows\platform-tools" 中的 "rm" 不会被误判。
# 只有当关键字作为独立的命令或参数出现时才会被阻止。
# 请用英文逗号 "," 分隔多个关键字。
# 示例: FORBIDDEN_COMMANDS=rm,del,format,rmdir,Remove-Item -Recurse -Force C:\
FORBIDDEN_COMMANDS=rm,del,format,rmdir

# --- 需要管理员授权的指令 ---
# 列出需要管理员验证码 (requireAdmin) 才能执行的指令中的关键字。
# 注意：智能检查机制同样适用于授权检查，路径中的关键字不会触发授权要求。
# 只有当关键字作为独立的命令或参数出现时才需要授权。
# 请用英文逗号 "," 分隔多个关键字。
# 示例: AUTH_REQUIRED_COMMANDS=net,shutdown,restart,New-Item
AUTH_REQUIRED_COMMANDS=net,shutdown,restart,Remove-Item,Clear-Content